#include<iostream>
#include<conio.h>
using namespace std;
int main()
{
	int count;
	for (count=1; count<=9; count++)
	{
		cout << count << ", ";
	}
	for (count = 10; count <= 30; count += 2)
	{
		if (count < 30)
			cout << count << ", ";
		else
			cout << count;
	}

	_getch();
	return 0;
}
